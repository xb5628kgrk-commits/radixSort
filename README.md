# radixSort
Generates a vector of random ints (default 1,000,000), sorts it sequentially (radix) and in parallel with Intel TBB, measures both runtimes, and prints size, threads, sequential/parallel times, speedup and efficiency. Run the compiled binary (e.g. radixSort).
# radixSort
# radixSort
